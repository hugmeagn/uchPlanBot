meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: /app/requirements.txt
  useCache: true
run:
  scriptName: /app/main.py
  command: null
  persistenceMount: /data
  containerPort: "80"
